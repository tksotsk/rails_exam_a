<%= render 'header' %>

<h3>以下の内容で、送信する。</h3>

<%= form_with(model: @post, url: posts_path, local: true) do |form| %>
  
  <%= form.hidden_field :content %>

  
  <p>内容:<%= @post.content %></p>
  <%= form.submit  "登録する" %>
<% end %>

<%= link_to '戻る', :back %>

<%= render 'footer' %>